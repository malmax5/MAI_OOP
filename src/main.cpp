#include <iostream>
#include <memory>
#include <vector>
// #include </home/cbf/MAI/OOP/Lab_3/includes/Vector.hpp>

int main()
{
    // Point<double> topLeft(0, 0);
    // Point<double> topRight(1, 1);
    // Point<double> downRight(2, 1);
    // Point<double> downLeft(3, 0);

    // Vector<double> a = std::move(topLeft);
    // std::cout << a;



    // Trapezoid<double> trap(topLeft, topRight, downRight, downLeft);
    // double area = (double)(trap);

    // double expectedArea = 2.0;

    // std::cout << (area == expectedArea);

    // Trapezoid<double> trap(topLeft, topRight, downRight, downLeft);
    // Point<double> centroid = trap.CalculateCentroid();

    // std::cout << trap << "\n";
    // std::cout << trap.CalculateCentroid() << "\n";

    // Point<double> topLeft2(1, 3);
    // Point<double> topRight2(2, 3);
    // Point<double> downRight2(3, 2);
    // Point<double> downLeft2(3, 1);

    // Trapezoid<double> trap2(topLeft2, topRight2, downRight2, downLeft2);
    // // Point<double> centroid2 = trap2.CalculateCentroid();

    // std::cout << trap2 << "\n";

    // Point<double> topLeft3(1, 1);
    // Point<double> topRight3(1, 2);
    // Point<double> downRight3(2, 3);
    // Point<double> downLeft3(3, 3);

    // Trapezoid<double> trap3(topLeft3, topRight3, downRight3, downLeft3);
    // // Point<double> centroid3 = trap3.CalculateCentroid();

    // std::cout << trap3 << "\n";

    // Point<double> topLeft4(1, 1);
    // Point<double> topRight4(0, 0);
    // Point<double> downRight4(1, 0);
    // Point<double> downLeft4(0, 1);

    // Trapezoid<double> trap4(topLeft4, topRight4, downRight4, downLeft4);
    // // Point<double> centroid4 = trap4.CalculateCentroid();

    // std::cout << trap4 << "\n";

    // Trapezoid<double> trap;
    // Square<double> sq;
    // Rectangle<double> re;
    // std::cin >> trap >> sq >> re;
    // std::cout << "Trapezoid: " << trap << "\nSquare: " << sq << "\nRectangle: " << re << "\n";
    // std::cout << "Trapezoid: " << (double)trap << "\nSquare: " << (double)sq << "\nRectangle: " << (double)re << "\n";
    // std::cout << "Trapezoid: " << trap.CalculateCentroid() << "\nSquare: " << sq.CalculateCentroid() << "\nRectangle: " << re.CalculateCentroid() << "\n";

    // std::vector<Figure<double>*> figures;
    // figures.push_back(&trap);
    // figures.push_back(&sq);
    // figures.push_back(&re);
    // std::cout << "\n";
    // for (int i = 0; i < figures.size(); i++)
    //     std::cout << (*figures[i]) << "\n";
    
    // for (int i = 0; i < figures.size(); i++)
    //     std::cout << figures[i]->CalculateArea() << "\n";
    
    // for (int i = 0; i < figures.size(); i++)
    //     std::cout << figures[i]->CalculateCentroid() << "\n";
}